# SubScope
Fast, cross-platform subdomain enumeration tool with async passive sources, strict normalization, source attribution, confidence scoring, and optional DNS validation with wildcard filtering.
